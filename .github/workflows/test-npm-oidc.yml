name: Test npm OIDC Authentication

on:
  push:
    branches:
      - '41-migrate-npm-publish-workflow-to-oidc-authentication'
  pull_request:
    branches:
      - '41-migrate-npm-publish-workflow-to-oidc-authentication'
  workflow_dispatch:
    inputs:
      test_version:
        description: 'Test version (e.g., 0.0.0-test)'
        required: true
        default: '0.0.0-test'
permissions:
  id-token: write
  contents: read
    
jobs:
  test-oidc-auth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'
          # OIDC will be used automatically if no NODE_AUTH_TOKEN is set
      - name: Verify Node.js and npm versions
        run: |
          echo "Node.js version: $(node --version)"
          echo "npm version: $(npm --version)"
          # Verify npm version supports OIDC (11.5.1+)
          NPM_VERSION=$(npm --version | cut -d. -f1,2)
          REQUIRED_VERSION="11.5"
          if [ "$(printf '%s\n' "$REQUIRED_VERSION" "$NPM_VERSION" | sort -V | head -n1)" != "$REQUIRED_VERSION" ]; then
            echo "⚠️  npm version $NPM_VERSION is below required $REQUIRED_VERSION for OIDC"
            echo "Installing latest npm..."
            npm install -g npm@latest
            echo "New npm version: $(npm --version)"
          else
            echo "✅ npm version $NPM_VERSION supports OIDC"
          fi
      - name: Install dependencies
        env:
          NODE_AUTH_TOKEN: ""
        run: npm ci
      - name: Build
        env:
          NODE_AUTH_TOKEN: ""
        run: npm run build
      - name: Verify OIDC prerequisites
        run: |
          echo "Verifying OIDC prerequisites..."
          echo ""
          echo "Checking environment variables:"
          if [ -n "$NODE_AUTH_TOKEN" ]; then
            echo "⚠️  WARNING: NODE_AUTH_TOKEN is set. This might interfere with OIDC."
            echo "   If you have NPM_TOKEN secret, remove it from repository secrets for OIDC to work."
          else
            echo "✅ NODE_AUTH_TOKEN is not set (correct for OIDC)"
          fi
          echo ""
          echo "Checking GitHub Actions OIDC environment variables:"
          if [ -n "$ACTIONS_ID_TOKEN_REQUEST_URL" ]; then
            echo "✅ ACTIONS_ID_TOKEN_REQUEST_URL is set"
          else
            echo "❌ ACTIONS_ID_TOKEN_REQUEST_URL is not set (required for OIDC)"
          fi
          if [ -n "$ACTIONS_ID_TOKEN_REQUEST_TOKEN" ]; then
            echo "✅ ACTIONS_ID_TOKEN_REQUEST_TOKEN is set"
          else
            echo "❌ ACTIONS_ID_TOKEN_REQUEST_TOKEN is not set (required for OIDC)"
          fi
          echo ""
          echo "GitHub Actions context:"
          echo "  Repository: ${{ github.repository }}"
          echo "  Workflow: ${{ github.workflow }}"
          echo "  Workflow file path: .github/workflows/test-npm-oidc.yml"
          echo ""
          echo "✅ All prerequisites for OIDC are met:"
          echo "   - npm version supports OIDC (11.6.2+)"
          echo "   - OIDC environment variables are available"
          echo "   - No token-based authentication is interfering"
          echo "   - Repository: checkfirst-ltd/nestjs-outlook"
          echo "   - Workflow: .github/workflows/test-npm-oidc.yml"
          echo ""
          echo "ℹ️  Note: According to npm docs, OIDC authentication only works during 'npm publish'"
          echo "   The 'npm whoami' command will NOT work with OIDC."
      - name: Test OIDC authentication with dry-run publish
        env:
          NODE_AUTH_TOKEN: ""
        run: |
          echo "Testing OIDC authentication with npm publish --dry-run..."
          echo ""
          echo "According to npm documentation:"
          echo "  - OIDC authentication only occurs during 'npm publish' operations"
          echo "  - 'npm whoami' will NOT reflect OIDC authentication status"
          echo "  - We'll test with 'npm publish --dry-run' to verify OIDC works"
          echo ""
          # Test OIDC authentication with dry-run publish
          # This is the only way to verify OIDC is working, as per npm docs
          npm publish --dry-run --access public || {
            echo ""
            echo "❌ Authentication failed!"
            echo ""
            echo "This usually means the trusted publisher is NOT configured correctly on npmjs.com."
            echo ""
            echo "📋 To fix this, configure the trusted publisher:"
            echo ""
            echo "1. Go to: https://www.npmjs.com/package/@checkfirst/nestjs-outlook/settings"
            echo "2. Click on 'Trusted Publishers' in the left sidebar"
            echo "3. Click 'Add Trusted Publisher'"
            echo "4. Select 'GitHub Actions'"
            echo "5. Enter the following EXACT values (case-sensitive):"
            echo "   - Repository: checkfirst-ltd/nestjs-outlook"
            echo "   - Workflow file: .github/workflows/test-npm-oidc.yml"
            echo "     (must include the .yml extension and match exactly)"
            echo "   - Environment: (leave blank for default)"
            echo "6. Click 'Add Trusted Publisher'"
            echo ""
            echo "⚠️  Important: npm does NOT verify your configuration when you save it."
            echo "   Double-check that the repository and workflow filename match EXACTLY."
            echo ""
            echo "💡 Troubleshooting:"
            echo "   - The workflow filename must match exactly, including .yml extension"
            echo "   - The repository name must be exactly: checkfirst-ltd/nestjs-outlook"
            echo "   - Wait 1-2 minutes after saving for the configuration to propagate"
            echo ""
            exit 1
          }
          echo ""
          echo "✅ OIDC authentication test passed!"
          echo "   The dry-run publish succeeded, which means OIDC is working correctly."

